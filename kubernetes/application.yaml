apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nycad-argocd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Oluwakomiyo/NYCAD-sndx.git'
    targetRevision: HEAD
    path: kubernetes
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: my-app-namespace
  syncPolicy:
    syncOptions:
      - CreateNamespace=true   
    automated:
      prune: true
      selfHeal: true